body {
    font-family: sans-serif;
    background-color: #f4f4f4;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    margin: 0;
}

.container {
    background-color: white;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    width: 90%;
    max-width: 600px;
    text-align: center;
}

#chat-container {
    display: flex;
    flex-direction: column;
    margin-top: 20px;
    border: 2px solid #007bff; /* Borde para el chat */
    border-radius: 8px;
}

#chat-history {
    padding: 10px;
    height: 300px;
    overflow-y: scroll;
    margin-bottom: 10px;
    display: flex;  /* Para alinear los mensajes */
    flex-direction: column;
}

#chat-history p {
    margin-bottom: 10px; /* Espacio entre mensajes */
    padding: 8px; /* Espacio interno */
    border-radius: 8px; /* Bordes redondeados */
    width: fit-content;
    max-width: 80%;
    word-wrap: break-word;
}

#chat-history .user-message {
    background-color: #e0f7fa;
    align-self: flex-end;
}

#chat-history .gemini-message {
    background-color: #f0f0f0;
    align-self: flex-start;
}


#chat-history hr {
    border: 0;
    border-top: 1px solid #eee; /* Línea divisora */
    margin: 5px 0;
}

#user-input {
    padding: 8px;
    margin-bottom: 10px;
    border: 1px solid #ddd;
    border-radius: 4px;
}

#send-button {
    padding: 8px 16px;
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    align-self: flex-end;
}